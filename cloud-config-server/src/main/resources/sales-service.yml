spring:
  application:
    name: sales-service
  cloud:
    stream:
#      bindings:
#        output-out-0:
#          destination: ticketChangeTopic
#          binder: local_rabbit
      bindings:
        input:
          destination: ticketOrderedInput
          contentType: application/json
          binder: local_rabbit
        output:
          destination: paymentSuccessInput
          contentType: application/json
          binder: local_rabbit
      binders:
        local_rabbit:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: localhost
                port: 5672
                username: guest
                password: guest


server:
  port: 5050